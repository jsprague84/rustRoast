# Ralph Progress: rustRoast Device Configuration
# Branch: ralph/device-configuration
# Started: 2026-02-28

## Codebase Patterns
- Migrations use `include_str!("../migrations/xxx.sql")` with split-by-semicolon execution and silent error handling (tables may already exist)
- Use `IF NOT EXISTS` for all CREATE TABLE/INDEX/TRIGGER statements for idempotency
- Triggers use `CREATE TRIGGER IF NOT EXISTS` syntax for updated_at auto-update
- Foreign keys use `ON DELETE CASCADE` for child tables, `ON DELETE SET NULL` for optional references
- AppState is in main.rs with clone-able fields (Arc<RwLock<...>>, SqlitePool, etc.)
- RoastSessionService follows the service pattern: struct with SqlitePool, async methods returning Result
- Route handlers are defined in main.rs (monolithic file) — control, session, profile, telemetry handlers all inline
- Models (request/response structs) are in models.rs with Serialize/Deserialize derives
- Tests use in-memory SQLite with the same migration pattern
- serde_json::Value in FromRow structs requires: sqlx `json` feature in Cargo.toml + `#[sqlx(json)]` attribute on the field
- When binding serde_json::Value to queries, convert to string first: `serde_json::to_string(&value)?`
- set_register_map uses a transaction: DELETE all existing + INSERT new entries for atomic replacement
- Route handlers can be separated into `routes/*.rs` files and merged via `Router::merge(device_routes())`
- AppState must be `pub(crate)` for route modules to extract it; service fields also need `pub(crate)`
- AppError pattern: struct with StatusCode + message, impl IntoResponse to emit JSON `{ error, status }`, impl From<anyhow::Error>
- Existing `/api/devices` was renamed to `/api/devices/registry` (in-memory MQTT device cache); new `/api/devices` serves persistent device config
- Standalone server components (Modbus TCP) go in `src/modbus.rs` (not services.rs which is for DB services)
- tokio-modbus 0.16 TCP server needs `features = ["tcp-server"]`; Service trait `call(&self)` requires Pin<Box<dyn Future>> for async ops
- When starting background tasks that clone Arc'd state, order matters: clone before the consumer loop that takes ownership
- mqtt_consumer_loop uses `#[allow(clippy::too_many_arguments)]` since it legitimately needs many Arc'd caches plus services
- Auto-discovery creates device + MQTT connection in two calls; if connection creation fails, device still exists (partial success is acceptable)
- Debounce HashMap<String, Instant> tracks per-device last-seen update times; no Arc needed since it's local to the consumer loop
- Axum 0.7 (matchit) correctly handles literal routes like `/api/devices/discovered` alongside parameterized `/api/devices/:id` — literal takes priority
- Frontend is React (not SvelteKit) with hash-based routing; pages are tab components in `src/pages/`, not file-based routes
- Use `@tanstack/react-query` (useQuery/useMutation) for data fetching; types/API functions export from `src/api/client.ts`
- App.tsx Tab union type + nav links + ErrorBoundary wrappers control page visibility
- `npm run build` (Vite) is the reliable quality check; `tsc --noEmit` has pre-existing failures

---

## 2026-02-28 - DEV-001
- Created device configuration database schema with 4 new tables
- Files changed:
  - `crates/server/migrations/003_device_configuration.sql` — New: device_profiles, devices, device_connections, modbus_register_maps tables with indexes, foreign keys, and updated_at triggers
  - `crates/server/src/main.rs` — Refactored migration loading from single include_str! to array iteration over multiple migration files
- **Learnings for future iterations:**
  - This branch (ralph/device-configuration) is based on main, which has older migration patterns (include_str! + split) vs the modernization branch which uses sqlx::migrate!()
  - The migration array pattern supports adding new migration files without restructuring the init_db function
  - device_profiles table must be created before devices table since devices has FK to device_profiles
  - SQLite `JSON` type is used for device_connections.config — stores protocol-specific config as serde_json::Value
---

## 2026-02-28 - DEV-002
- Created typed Rust structs for all device-related database tables in models.rs
- Files changed:
  - `crates/server/src/models.rs` — Added Device, DeviceProfile, DeviceConnection, ModbusRegisterMap (FromRow structs), DeviceWithConnections (response struct), Create/Update request structs, typed config structs (MqttConnectionConfig, WebSocketConnectionConfig, ModbusTcpConnectionConfig), and enums (DeviceStatus, Protocol, ModbusRegisterType, ModbusDataType) with Display/FromStr/SQLx implementations
- **Learnings for future iterations:**
  - Follow the SessionStatus pattern for enum implementations: Type<sqlx::Sqlite> + Decode + Encode + Display + FromStr
  - serde_json::Value requires the sqlx `json` feature AND `#[sqlx(json)]` attribute on the FromRow field to decode JSON columns
  - Dead code warnings are expected for model structs before the service layer (DEV-003) consumes them
  - DeviceProfile uses f64 for REAL columns (matching SQLite's float type), while session models use f32 — be consistent within the device configuration domain
  - UpdateConnectionRequest is separate from CreateConnectionRequest to allow partial updates (all fields Optional)
---

## 2026-02-28 - DEV-003
- Implemented DeviceService with full CRUD operations for devices, profiles, connections, and register maps
- Added 14 integration tests covering all service methods including cascade deletes and transactional register map replacement
- Files changed:
  - `crates/server/src/services.rs` — Added DeviceService with 17 methods: list/get/create/update/delete for devices, list/get/create/delete for device profiles, add/update/remove for connections, get/set for register maps, plus update_device_status and update_last_seen. Added comprehensive test suite.
  - `crates/server/src/main.rs` — Added DeviceService to AppState, imported DeviceService, fixed pre-existing clippy warnings (unused session_id variables)
  - `crates/server/src/models.rs` — Added `#[sqlx(json)]` attribute to DeviceConnection.config field, added `#[allow(dead_code)]` to model structs pending route handler consumption
  - `crates/server/Cargo.toml` — Added `json` feature to sqlx dependency for JSON column support
- **Learnings for future iterations:**
  - sqlx `json` feature must be added to Cargo.toml AND `#[sqlx(json)]` must be placed on the FromRow field for JSON columns
  - When binding serde_json::Value to INSERT/UPDATE queries, serialize to string first with `serde_json::to_string(&value)?`
  - `#[allow(dead_code)]` on a struct doesn't suppress warnings for its impl methods — must be placed on the `impl` block itself
  - Dynamic update queries (conditional SET clauses) follow the same pattern as RoastSessionService.update_session: build query string, then bind in same order as SET clauses
  - set_register_map should use a SQLite transaction (`self.db.begin()`) for atomic delete-then-insert replacement
  - Pre-existing clippy issues should be fixed opportunistically to keep CI green
---

## 2026-02-28 - DEV-004
- Added device management REST API endpoints with consistent JSON error responses
- Created new routes module (`routes/`) to organize device handlers separately from main.rs
- Files changed:
  - `crates/server/src/routes/mod.rs` — New: module re-exporting device_routes()
  - `crates/server/src/routes/devices.rs` — New: 15 route handlers for device CRUD, device profile CRUD, connection management, and register map endpoints. Includes AppError type for consistent JSON error responses.
  - `crates/server/src/main.rs` — Added `mod routes`, imported `device_routes`, merged device routes into router, made AppState `pub(crate)`, renamed old `/api/devices` (in-memory registry) to `/api/devices/registry`
  - `crates/server/src/models.rs` — Removed `#[allow(dead_code)]` from model/request structs now consumed by route handlers
  - `crates/server/src/services.rs` — Removed `#[allow(dead_code)]` from DeviceService struct/impl, added it to individual methods not yet used (get_device_by_device_id, update_device_status, update_last_seen)
- **Learnings for future iterations:**
  - Route modules need AppState to be `pub(crate)` and service fields to be `pub(crate)` for extraction
  - `Router::merge()` is the cleanest way to add a batch of routes from a separate module
  - AppError with `From<anyhow::Error>` and `IntoResponse` enables `Result<T, AppError>` return types in handlers — much cleaner than match/error tuples
  - The existing `/api/devices` endpoint returns in-memory MQTT device registry data; renamed to `/api/devices/registry` to avoid conflict with the new persistent device management endpoints
  - Register map and connection routes verify device existence before operating, returning 404 if the device doesn't exist
- tokio-modbus 0.16 `read_input_registers()` returns `Result<Result<Vec<u16>, ExceptionCode>>` — needs double `?` (`await??`) to unwrap both layers
- Connection test endpoints return 200 with `success: false` for failed tests (not 4xx) since a failed test is a valid response
- telemetry_cache stores `(serde_json::Value, u64)` where u64 is epoch seconds; make field `pub(crate)` for route module access

---

## 2026-02-28 - DEV-005
- Added connection testing endpoint supporting MQTT, Modbus TCP, and WebSocket protocols
- MQTT test checks in-memory telemetry cache for recent telemetry (within 30 seconds) for the specified device_id
- Modbus TCP test uses tokio-modbus to connect with a 5-second timeout, reads input registers 0x0000-0x0001 (bean_temp)
- WebSocket test uses tokio-tungstenite to attempt handshake and wait for first message with 5-second timeout
- All test failures return HTTP 200 with `success: false` (not 4xx/5xx)
- Files changed:
  - `crates/server/Cargo.toml` — Added tokio-modbus 0.16, tokio-tungstenite 0.21, futures-util 0.3 dependencies
  - `crates/server/src/models.rs` — Added TestConnectionRequest and TestConnectionResponse structs
  - `crates/server/src/routes/devices.rs` — Added test_connection handler with per-protocol test functions (test_mqtt_connection, test_modbus_connection, test_websocket_connection), registered POST /api/devices/test-connection route
  - `crates/server/src/main.rs` — Made telemetry_cache field pub(crate) for route module access
- **Learnings for future iterations:**
  - tokio-modbus 0.16 `read_input_registers()` returns `Result<Result<Vec<u16>, ExceptionCode>>` — requires double `?` to unwrap both the I/O error and the Modbus exception
  - Connection test endpoints should return 200 with success: false for failures, since a test failure is a valid result (not an API error)
  - The telemetry_cache key is the device_id string, and the value tuple has (json_payload, epoch_seconds) — check age by comparing epoch_secs against current time
  - For WebSocket testing, futures_util::StreamExt is needed to call `.next()` on the split read stream
---

## 2026-02-28 - DEV-006
- Integrated Modbus TCP server into the backend using tokio-modbus 0.16 tcp-server feature
- Server is disabled by default, enabled via RUSTROAST_MODBUS_ADDR env var (e.g. "0.0.0.0:502")
- Default device_id configurable via RUSTROAST_MODBUS_DEVICE_ID env var (defaults to "esp32-001")
- Input registers (read-only): 0x0000-0x0001 bean_temp (float32), 0x0002-0x0003 env_temp (float32), 0x0004-0x0005 rate_of_rise (float32), 0x0006 heater_pwm (uint16), 0x0007 fan_pwm (uint16)
- Holding registers (read-write): 0x0000-0x0001 setpoint (float32), 0x0002 fan_pwm_setpoint, 0x0003 heater_pwm_setpoint, 0x0004 control_mode, 0x0005 heater_enable, 0x000C emergency_stop
- Writes to holding registers publish corresponding MQTT control commands via MqttService
- Each TCP connection gets its own Service instance via factory closure; all share Arc'd state with Axum server
- Files changed:
  - `crates/server/Cargo.toml` — Added `tcp-server` feature to tokio-modbus dependency
  - `crates/server/src/modbus.rs` — New: RoasterModbusService implementing tokio_modbus::server::Service, ModbusSharedState, input/holding register read/write logic, MQTT command publishing, start_modbus_server() entry point
  - `crates/server/src/main.rs` — Added `mod modbus`, call to `modbus::start_modbus_server()` before MQTT consumer loop (to clone telemetry_cache before it's moved)
- **Learnings for future iterations:**
  - tokio-modbus 0.16 TCP server requires `features = ["tcp-server"]` in Cargo.toml — the default features only include the TCP client
  - The Service trait's `call(&self)` takes immutable reference; use Pin<Box<dyn Future + Send>> as the Future type to perform async operations (reading from tokio::sync::RwLock, publishing MQTT)
  - Modbus TCP server is a standalone module (modbus.rs) rather than part of services.rs, since it's a server component not a database service
  - When starting Modbus server before mqtt_consumer_loop, clone telemetry_cache first since mqtt_consumer_loop takes ownership
  - Float32 values are encoded IEEE 754 big-endian across 2 consecutive 16-bit registers: high word at lower address, low word at higher address
  - Holding registers 0x0006-0x000B are gap/reserved registers (no MQTT mapping) — reads return 0, writes are no-ops
---

## 2026-02-28 - DEV-007
- Updated MQTT consumer to integrate with persistent device configuration
- Auto-discovery: unknown device_ids arriving via MQTT auto-create a device entry (status 'pending') with default MQTT connection config
- Last-seen debouncing: update_last_seen called at most once per 10 seconds per device using HashMap<String, Instant>
- Disabled devices: telemetry is cached but session_telemetry recording is skipped
- Added GET /api/devices/discovered endpoint returning devices with status 'pending'
- Files changed:
  - `crates/server/src/main.rs` — Added DeviceService to mqtt_consumer_loop, auto-discovery logic, last-seen debouncing, disabled device check, device_service.clone() for background task
  - `crates/server/src/routes/devices.rs` — Added list_discovered_devices handler and GET /api/devices/discovered route
  - `crates/server/src/services.rs` — Removed dead_code annotations from get_device_by_device_id and update_last_seen (now consumed by MQTT consumer)
- **Learnings for future iterations:**
  - Adding a parameter to mqtt_consumer_loop (now 8 params) triggers clippy::too_many_arguments — use `#[allow]` since it's a background task with legitimately many dependencies
  - DeviceService must be `.clone()`d before being moved into AppState if it's also needed by the background consumer
  - Auto-discovery uses get_device_by_device_id (looks up by device_id string, not internal UUID) — important distinction from get_device (which uses internal id)
  - The device status check happens once per telemetry message; for high-frequency telemetry this adds a DB lookup per message. A future optimization could cache device statuses in memory.
  - Axum 0.7 route ordering: literal path segments like `/api/devices/discovered` take priority over parameterized `/api/devices/:id` regardless of registration order
- The frontend is React (not SvelteKit) with hash-based routing in App.tsx — pages are tab components, not file-based routes
- Frontend uses `@tanstack/react-query` for data fetching; new pages follow the `useQuery` pattern from Sessions.tsx
- The `api` object in `src/api/client.ts` is the single source for all API calls and type exports
- App.tsx Tab type union controls navigation; new pages need: Tab value, nav link, ErrorBoundary wrapper, and import
- `npm run build` (Vite) is the reliable quality check; `tsc --noEmit` has pre-existing failures due to missing type declarations
- When adding new sub-pages under devices tab, add explicit match in handleNavigate BEFORE the generic `devices/` catch-all, AND exclude from device detail rendering condition
---

## 2026-02-28 - DEV-008
- Created device list page in the dashboard with full device management UI
- Added persistent device configuration types and API functions to client.ts
- Rewrote Devices.tsx from simple picker to full management page with:
  - Pending/discovered devices section (amber border, configure button)
  - Configured devices grid (card layout with status badges, relative time)
  - Status badges using existing CSS classes (status-online, status-warning, status-offline)
  - Empty state with messaging
  - Add Device button linking to wizard route
- Updated App.tsx navigation with Devices tab between Sessions and Profiles
- Updated api.devices() to call `/api/devices/registry` (matching backend rename from DEV-004)
- Files changed:
  - `apps/dashboard/src/api/client.ts` — Added ConfiguredDevice, DeviceConnection, ConfiguredDeviceWithConnections types; added listConfiguredDevices, getConfiguredDevice, getDiscoveredDevices, deleteConfiguredDevice API functions; updated devices() to use /api/devices/registry
  - `apps/dashboard/src/pages/Devices.tsx` — Complete rewrite: DeviceCard, PendingDeviceRow components, status badges, relative time formatting, React Query data fetching
  - `apps/dashboard/src/App.tsx` — Added 'devices' to Tab type, imported Devices page, added nav link and ErrorBoundary, updated handleNavigate for device/* sub-routes
- **Learnings for future iterations:**
  - The frontend is React + Vite, NOT SvelteKit — PRD references to SvelteKit patterns should be adapted to React equivalents
  - Pages are components rendered by hash-based tab switching in App.tsx, not file-based routes
  - The `list_devices` backend endpoint returns `Vec<Device>` without connections — connection/protocol info requires individual `get_device` calls
  - The api.devices() function was pointing to the old `/api/devices` URL; it needed updating to `/api/devices/registry` since the old route was renamed in DEV-004
  - `npm install` is required before `npm run build` if node_modules is stale
  - Pre-existing chunk size warning (ECharts ~1MB) can be ignored
---

## 2026-02-28 - DEV-009
- Created device setup wizard with multi-step UI and Step 1 (Discovery & Identification) fully functional
- Wizard has 4-step indicator (Device, Connections, Configuration, Review) with visual progress
- Step 1 shows discovered devices section (from /api/devices/discovered) with MQTT registry enrichment (IP, firmware, RSSI)
- Manual setup form with device_id (required), name (required), description, location fields
- Device profile dropdown populated from /api/device-profiles
- Selecting a discovered device auto-fills device_id and name
- Next button disabled until required fields are filled
- Steps 2-4 are placeholder shells for DEV-010/011/012
- Files changed:
  - `apps/dashboard/src/pages/DeviceWizard.tsx` — New: multi-step wizard component with StepIndicator, DiscoveredDeviceRow, Step1Device sub-components, WizardFormData type
  - `apps/dashboard/src/api/client.ts` — Added DeviceProfile type, CreateDeviceRequest type, listDeviceProfiles and createDevice API functions
  - `apps/dashboard/src/App.tsx` — Added deviceView state for device sub-routing, DeviceWizard import, routing logic for devices/new path with optional device_id query param, hash change resets deviceView
- **Learnings for future iterations:**
  - App.tsx sub-routing uses a string state (deviceView) similar to sessionId — null = list view, 'devices/new...' = wizard, other = detail page
  - handleNavigate must check for exact 'devices' path BEFORE the `devices/` prefix catch-all to avoid the list view always showing
  - Hash routing onHash handler should reset sub-view states (deviceView, sessionId) when navigating via nav links to prevent stale sub-views
  - The DeviceWizard exports WizardFormData type so future step implementations (DEV-010/011/012) can extend it with additional fields
  - Discovered devices are enriched with MQTT registry data (IP, firmware, RSSI) by cross-referencing with /api/devices/registry
---

## 2026-03-01 - DEV-010
- Implemented Step 2 (Connection Configuration) of the device setup wizard
- Three protocol toggle cards: MQTT, WebSocket, and Modbus TCP with enable/disable toggles
- MQTT fields: topic prefix (auto-populated from device_id), QoS dropdown (0/1/2)
- WebSocket fields: URL, reconnect interval
- Modbus TCP fields: host, port, unit ID, poll interval
- Each protocol has a "Test Connection" button calling POST /api/devices/test-connection
- Test status: loading spinner during test, green checkmark with latency on success, red X with error on failure
- At least one protocol must be enabled and tested successfully to advance to Step 3
- Back button returns to Step 1 preserving form data
- Added testConnection and addConnection API functions to client.ts
- Added TestConnectionRequest, TestConnectionResponse, CreateConnectionRequest types
- Connection test state lifted to parent DeviceWizard so canAdvanceStep2 gate works correctly
- Files changed:
  - `apps/dashboard/src/api/client.ts` — Added testConnection, addConnection API functions; TestConnectionRequest, TestConnectionResponse, CreateConnectionRequest types
  - `apps/dashboard/src/pages/DeviceWizard.tsx` — Added ConnectionConfig, ConnectionTestState types, ProtocolCard component, Step2Connections component, handleTestConnection in parent, extended WizardFormData with connections, canAdvanceStep2 gate, MQTT topic_prefix auto-sync with device_id
- **Learnings for future iterations:**
  - Connection test state must be lifted to the parent (DeviceWizard) when the parent needs it for navigation gating (canAdvanceStep2); keeping it local in Step2Connections would make it invisible to the parent
  - MQTT topic_prefix auto-sync: updateFormData detects device_id changes and updates mqtt.topic_prefix to `roaster/{device_id}` unless connections are explicitly being set
  - The ProtocolCard component is reusable for the device detail/edit page (DEV-013) connection tab
  - CSS spin animation is injected via a `<style>` tag inside the component since there's no global CSS injection in this React setup
  - `npm run build` (Vite) remains the reliable quality check; ECharts chunk size warning is expected
---

## 2026-03-01 - DEV-011
- Implemented Step 3 (Device Configuration) of the device setup wizard
- Four collapsible sections: Control Defaults, Safety Limits, PID Parameters (collapsed by default), Modbus Register Map (conditional)
- Control Defaults: control mode radio (manual/auto), default setpoint (0-300), default fan PWM (0-255)
- Safety Limits: max temperature (100-350, default 240), minimum fan PWM for heater (0-255, default 100)
- PID Parameters: Kp (default 15.0), Ki (default 1.0), Kd (default 25.0) with step increments
- Modbus Register Map: full table editor with address (hex), name, type, data type, byte order, scale, offset, unit, writable toggle
- "Load Default Map" button populates standard coffee roaster register layout (5 input + 6 holding registers matching modbus.rs)
- Add/remove row support for custom register entries
- CollapsibleSection reusable component with toggle arrow
- Extended WizardFormData with `config: DeviceConfigData` and `register_map: ModbusRegisterEntry[]` fields
- Files changed:
  - `apps/dashboard/src/pages/DeviceWizard.tsx` — Added ModbusRegisterEntry, DeviceConfigData types, DEFAULT_REGISTER_MAP constant, CollapsibleSection, RegisterMapEditor, Step3Configuration components, updated initial formData state
- **Learnings for future iterations:**
  - CollapsibleSection uses local useState for open/close — suitable for sections that don't need external control
  - The default register map mirrors the modbus.rs register layout exactly: input regs 0x0000-0x0007 (bean_temp, env_temp, rate_of_rise, heater_pwm, fan_pwm) and holding regs 0x0000-0x000C (setpoint, fan_pwm_setpoint, heater_pwm_setpoint, control_mode, heater_enable, emergency_stop)
  - formatHex utility converts numeric addresses to 0x-prefixed hex strings for display; parseInt(val, 16) converts back
  - Register map table uses inline editing for each cell — overflowX: auto handles narrow viewports
  - The "Load Default Map" confirms replacement if entries already exist (window.confirm)
---

## 2026-03-01 - DEV-012
- Implemented Step 4 (Review & Save) of the device setup wizard
- Read-only summary of all configuration organized in sections: Device Info, Connections, Configuration, Modbus Register Map
- Each connection shows test status (passed/not tested) with colored badge
- Register map summary table shows address (hex), name, type, data type, writable status
- Save action performs sequential API calls: POST /api/devices, POST /api/devices/:id/connections (per protocol), PUT /api/devices/:id/register-map
- Partial failure handling: if device created but connections/register map fail, shows error and redirects to device edit page after 2s
- Save button shows "Saving..." loading state, Back button disabled during save
- Error messages displayed inline in a red alert box without losing form data
- On success, redirects to /devices list
- Added setRegisterMap and CreateRegisterMapEntry to api/client.ts
- Files changed:
  - `apps/dashboard/src/pages/DeviceWizard.tsx` — Added Step4Review component (ReviewSection, ReviewField helpers), handleSave async function with sequential API calls and partial failure handling, saving/saveError state, loading state on Save button, Back button disabled during save
  - `apps/dashboard/src/api/client.ts` — Added setRegisterMap API function and CreateRegisterMapEntry type
- **Learnings for future iterations:**
  - Sequential API calls (create device -> add connections -> set register map) need careful error handling for partial success — device may exist even if later calls fail
  - The `onSave` prop on Step4Review is not used directly by the component (save is triggered from the parent's nav button); the prop is included for flexibility but handleSave is called from the parent's button onClick
  - CreateRegisterMapEntry fields must match the backend Rust struct exactly: register_type, address, name, data_type are required; byte_order, scale_factor, offset, unit, description, writable are optional
  - `npm run build` (Vite) remains the reliable quality check; ECharts chunk size warning is expected and pre-existing
---

## 2026-03-01 - DEV-013
- Created device detail and edit page with tabbed interface (Overview, Connections, Configuration, Register Map)
- Overview tab: editable name/description/location, device profile selector, status badge, last seen, enable/disable/delete quick actions, delete confirmation dialog
- Connections tab: lists all connections with protocol icon/label/status, inline editing of connection config, test connection button per connection, remove connection with confirmation, add new connection form with protocol-specific fields
- Configuration tab: collapsible sections for Control Defaults (mode/setpoint/fan PWM), Safety Limits (max temp/min fan PWM), PID Parameters (Kp/Ki/Kd), each with individual Save buttons, "Apply to Device" button sends PID values to live device via control API
- Register Map tab: conditional on Modbus connection existing, full table editor with load default map, add/remove rows, save map to API, fetches existing map from GET /api/devices/:id/register-map
- Added missing API functions to client.ts: updateDevice, updateConnection, removeConnection, getRegisterMap
- Added types: UpdateDeviceRequest, UpdateConnectionRequest, ModbusRegisterMapEntry
- Wired DeviceDetail into App.tsx routing: renders when deviceView is set but doesn't start with 'devices/new'
- Files changed:
  - `apps/dashboard/src/pages/DeviceDetail.tsx` — New: full device detail/edit page with OverviewTab, ConnectionsTab (ConnectionRow, AddConnectionForm), ConfigurationTab, RegisterMapTab, reusable SaveButton and CollapsibleSection components
  - `apps/dashboard/src/api/client.ts` — Added updateDevice, updateConnection, removeConnection, getRegisterMap API functions; added UpdateDeviceRequest, UpdateConnectionRequest, ModbusRegisterMapEntry types
  - `apps/dashboard/src/App.tsx` — Imported DeviceDetail, added rendering condition for device detail view when deviceView is a device ID
- **Learnings for future iterations:**
  - The deviceView state in App.tsx serves as a sub-router: null = list, 'devices/new...' = wizard, anything else = device detail ID
  - Devices.tsx navigation uses `device/${device.id}` (internal UUID) which gets parsed by handleNavigate into deviceView; this is the internal ID, not device_id
  - SaveButton component with idle/saving/saved/error states and auto-reset timers is a useful pattern for section-level saves
  - ConnectionRow uses local state for config editing — lifting to parent would be needed if cross-connection validation were required
  - RegisterMapTab initializes local state from API data once (initialized flag pattern) to avoid overwriting user edits on refetch
  - The `import type` syntax is needed for importing types from DeviceWizard.tsx (ModbusRegisterEntry) to avoid circular deps
---

## 2026-03-01 - DEV-014
- Created device profile management UI page at /devices/profiles
- Lists all device profiles with name, description, settings summary, and device count per profile
- Profile cards show control defaults (mode/setpoint/fan PWM), safety limits (max temp/min fan PWM), and PID parameters (Kp/Ki/Kd)
- "Create Profile" button opens modal form with fields: name, description, control mode, setpoint, fan PWM, max temp, min fan PWM, telemetry interval, PID Kp/Ki/Kd
- Edit and delete actions per profile; delete warns if devices are using the profile
- Device count per profile computed client-side by cross-referencing with the configured devices list
- Added "Device Profiles" button to Devices list page header for navigation
- Added createDeviceProfile, deleteDeviceProfile, getDeviceProfile API functions and CreateDeviceProfileRequest type to client.ts
- Routed via deviceView === 'devices/profiles' in App.tsx (consistent with devices/new pattern)
- Files changed:
  - `apps/dashboard/src/pages/DeviceProfiles.tsx` — New: ProfileCard, ProfileForm, DeviceProfiles components
  - `apps/dashboard/src/api/client.ts` — Added createDeviceProfile, deleteDeviceProfile, getDeviceProfile API functions; added CreateDeviceProfileRequest type
  - `apps/dashboard/src/App.tsx` — Imported DeviceProfiles, added routing for devices/profiles path, excluded from device detail rendering
  - `apps/dashboard/src/pages/Devices.tsx` — Added "Device Profiles" navigation button in page header
- **Learnings for future iterations:**
  - deviceView routing needs explicit string matches for sub-pages (e.g., 'devices/profiles') checked BEFORE generic prefix matches
  - Device profile editing uses delete-then-recreate since the backend doesn't expose a PUT /api/device-profiles/:id endpoint
  - Device count per profile is computed client-side since the backend list_profiles doesn't return usage counts — fetches both profiles and devices lists
  - The 'devices/profiles' path must be excluded from the device detail rendering condition alongside 'devices/new'
---

## 2026-03-01 - DEV-015
- Added device selector to dashboard and session creation, replacing hardcoded device_id
- App.tsx fetches active configured devices via useQuery on /api/devices?status=active with auto-selection logic
- UnifiedDashboard shows device selector dropdown when multiple active devices exist, no-devices setup prompt when none exist
- Sessions CreateSessionForm shows device dropdown when multiple active devices, allowing user to pick which device for the session
- Dashboard footer stats now show device name instead of raw device_id
- Files changed:
  - `apps/dashboard/src/App.tsx` — Added useQuery for active devices, auto-select effect, pass activeDevices/onDeviceChange/onNavigate to UnifiedDashboard and Sessions
  - `apps/dashboard/src/pages/UnifiedDashboard.tsx` — Added activeDevices/onDeviceChange/onNavigate props, no-devices empty state, device selector dropdown in header, device name in footer stats
  - `apps/dashboard/src/pages/Sessions.tsx` — Added activeDevices prop to Sessions and CreateSessionForm, device dropdown in session creation form when multiple devices
- **Learnings for future iterations:**
  - App.tsx auto-select effect should not include `deviceId` in deps to avoid infinite loop — it reads deviceId inside but only reacts to activeDevices changes
  - UnifiedDashboard no-devices state returns early before the rest of the component, keeping the happy path clean
  - CreateSessionForm device dropdown only shown when activeDevices.length > 1 (single device auto-uses the passed deviceId)
  - The activeDevices query uses staleTime: 30000 to avoid excessive refetching since device configuration changes infrequently
---
