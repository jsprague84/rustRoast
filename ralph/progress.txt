# Ralph Progress Log
Started: Mon Mar  2 09:04:16 PM CST 2026

## Codebase Patterns
- Migration files in `crates/server/migrations/` — each ALTER TABLE must be a separate statement (SQLite fails silently if column exists)
- Migrations 001 and 003 were included in `init_db()` but 002 (roast_events) was missing — fixed; always include all migration files
- Tests use in-memory SQLite via `setup_test_db()` in services.rs — must mirror the same migration list as `init_db()`
- `RoastSession` model uses `#[derive(FromRow)]` — fields MUST match column order/names in `roast_sessions` table
- Frontend types: `RoastSession` interface is in `$lib/api/client.ts`, `SessionWithTelemetry` extends it in `$lib/types/session.ts`
- `query_as::<_, RoastSession>` with `RETURNING *` requires all model fields to have matching DB columns
- Chart.svelte uses tree-shaken ECharts — must register new components (e.g., LegendComponent) and add to ECOption type union
- ECharts markLine label.position requires literal type, not plain `string`

---

## 2026-03-02 - AP-001
- Implemented enhanced session completion statistics in `complete_session()`
- Files changed:
  - `crates/server/migrations/004_session_statistics.sql` (new) — 7 ALTER TABLE statements adding columns
  - `crates/server/src/main.rs` — added migrations 002 + 004 to init_db
  - `crates/server/src/models.rs` — added 7 new Option fields to RoastSession
  - `crates/server/src/services.rs` — rewrote complete_session() with DTR, weight_loss, max_ror, per-phase avg_ror, drying_end fields; added avg_ror_in_range() helper; added test; fixed migration list in setup_test_db()
  - `apps/dashboard/src/lib/api/client.ts` — added new fields to RoastSession interface
- **Learnings for future iterations:**
  - Migration 002_roast_events.sql existed but was NOT included in init_db() or setup_test_db() — needed to add it
  - SQLite doesn't support `ALTER TABLE ADD COLUMN IF NOT EXISTS` — use separate statements that silently fail
  - `RETURNING *` with `query_as` means ALL model fields must have corresponding DB columns
  - Phase boundaries come from roast_events table: drying_end → first_crack_start → drop
  - The COALESCE pattern (e.g. `COALESCE(?, first_crack_time)`) preserves manually-set values when auto-computed values are NULL
---

## 2026-03-02 - AP-002
- Implemented AUC (Area Under the Curve) computation using trapezoidal rule
- Files changed:
  - `crates/server/migrations/005_auc_value.sql` (new) — ALTER TABLE adding auc_value column
  - `crates/server/src/main.rs` — added migration 005, seeded auc_base_temp and auc_start_event settings
  - `crates/server/src/models.rs` — added auc_value: Option<f32> field
  - `crates/server/src/services.rs` — added compute_auc() method, integrated into complete_session(), added settings table + seeds to setup_test_db(), added AUC test
  - `apps/dashboard/src/lib/api/client.ts` — added auc_value to RoastSession interface
- **Learnings for future iterations:**
  - `setup_test_db()` must also create settings table and seed defaults — migrations don't include the inline CREATE TABLE from init_db
  - `sqlx::query_scalar::<_, String>()` is a clean way to fetch a single setting value
  - AUC is computed in °C·s internally then divided by 60 for °C·min units
  - Trapezoidal rule uses `rows.windows(2)` for pairwise iteration
---

## 2026-03-02 - AP-003
- Seeded RoR smoothing settings: ror_window_seconds='30', ror_smoothing_algorithm='moving_average'
- Files changed:
  - `crates/server/src/main.rs` — added 2 INSERT OR IGNORE statements in init_db
  - `crates/server/src/services.rs` — added 2 settings to setup_test_db seed array
- **Learnings for future iterations:**
  - Settings-only stories are minimal: just INSERT OR IGNORE in init_db + mirror in setup_test_db
  - Supported algorithms (moving_average, weighted_moving_average, savitzky_golay) are just string values — no backend validation
---

## 2026-03-02 - AP-004
- Added simulated RoR curve, phase markers, and DTR preview to ProfileDesigner
- Files changed:
  - `apps/dashboard/src/lib/components/Chart.svelte` — registered LegendComponent for ECharts legend support
  - `apps/dashboard/src/lib/components/ProfileDesigner.svelte` — added interpolateTemp(), computeRoR(), findTempCrossing() helpers; dual Y-axis (temp + RoR); Simulated RoR series (green dashed); DRY/FC markLine phase markers; DTR info bar below chart
- **Learnings for future iterations:**
  - ECharts markLine `label.position` must be a specific literal type (e.g., `'insideEndTop'`), not just `string` — use `as const` or inline type
  - Chart.svelte ECOption type is a ComposeOption — add new component options to the union when registering new ECharts components
  - `yAxis` can be an array for dual-axis charts; second series uses `yAxisIndex: 1`
  - RoR simulation uses central difference: `(BT[t+dt] - BT[t-dt]) / (2*dt) * 60` for °C/min
  - `grid.right` must be increased (20→55) to make room for the second Y-axis labels
  - `silent: true` on a series prevents it from capturing mouse events (critical for the draggable point overlay)
---

## 2026-03-02 - AP-005
- Added fan speed stepped line series to profile designer chart
- Files changed:
  - `apps/dashboard/src/lib/components/ProfileDesigner.svelte` — added fan speed series (step:'end', purple, area fill 15%), dynamic right Y-axis range (0-100 when fan data present), tooltip unit for fan speed
- **Learnings for future iterations:**
  - ECharts `step: 'end'` creates a stepped line — use `as const` for TypeScript literal types
  - Spread syntax `...(condition ? [item] : [])` is clean for conditionally adding series to an array
  - When sharing a Y-axis between RoR and fan speed, fix the range to 0-100 (fan percentage) since RoR values (5-25) still display fine in that range
  - Only render fan speed series when there are non-null fan_speed points to avoid empty legend entries
---

## 2026-03-02 - AP-006
- Added profile transposer dialog for scaling/shifting profile curves
- Files changed:
  - `apps/dashboard/src/lib/components/ProfileTransposer.svelte` (new) — modal dialog with time scale, temp shift, temp scale sliders; preview chart showing original vs transposed curves with simulated RoR
  - `apps/dashboard/src/lib/components/ProfileDesigner.svelte` — added Transpose button (ArrowRightLeft icon, visible when ≥2 points), ProfileTransposer import, showTransposer state, onApply callback that replaces points array
- **Learnings for future iterations:**
  - Modal pattern: fixed inset-0 z-50 div with bg-black/60 backdrop, onclick on backdrop to close
  - `$derived.by()` works well for computing transposed points reactively from slider state
  - `computeSimpleRoR()` in transposer uses forward-difference (simpler than central-difference in designer) — both approaches are valid for preview
  - The onApply callback maps TransposerPoint[] back to DesignerPoint[] to ensure type compatibility
  - Show transpose button conditionally (≥2 points) since transposing a single point is meaningless
---

## 2026-03-02 - AP-007
- Added configurable RoR smoothing with 3 algorithms and adjustable window size
- Files changed:
  - `apps/dashboard/src/lib/stores/telemetry.ts` — added _rorWindowMs/_rorAlgorithm writable stores; implemented rorMovingAverage, rorWeightedMovingAverage, rorSavitzkyGolay functions; rateOfRise now derives from [_history, _rorWindowMs, _rorAlgorithm]; exported setRorConfig(), loadRorSettings()
  - `apps/dashboard/src/routes/+layout.svelte` — calls loadRorSettings() on mount
  - `apps/dashboard/src/routes/settings/+page.svelte` — added Rate of Rise section with window size slider and algorithm dropdown
- **Learnings for future iterations:**
  - svelte/store `derived` can take an array of stores: `derived([storeA, storeB], ([$a, $b], set) => ...)`
  - Weighted moving average uses weighted linear regression: w[i] = (i+1)/sum(1..n), then weighted least-squares slope
  - Savitzky-Golay: center time around midpoint for numerical stability; derivative at t=0 is just the `b` coefficient from y = a + bt + ct²
  - Cramer's rule works for the 3x3 normal equations — no need for a full matrix library
  - Import from $lib/api/client.js in telemetry.ts is safe (no circular dependency)
---

## 2026-03-02 - AP-008
- Created PhaseStatsPanel.svelte with enhanced phase statistics (replaces PhaseTimingTable on session detail)
- Files changed:
  - `apps/dashboard/src/lib/components/PhaseStatsPanel.svelte` (new) — phase table with duration, %, avg RoR, temp rise, DTR; Artisan-style colors; accepts optional pre-computed stats
  - `apps/dashboard/src/routes/sessions/[id]/+page.svelte` — replaced PhaseTimingTable with PhaseStatsPanel, passes avg_ror_* from session object
  - `apps/dashboard/src/routes/+page.svelte` — added PhaseStatsPanel to dashboard sidebar, stores sessionEvents from API alongside landmarks
- **Learnings for future iterations:**
  - PhaseStatsPanel derives avg RoR either from pre-computed session fields (historical) or from tempRise/duration (live)
  - Dashboard already loads events via loadLandmarks() — store the raw RoastEvent[] for reuse by other components
  - Phase colors: Drying=#22c55e, Maillard=#eab308, Development=#92400e (Artisan convention)
  - PhaseTimingTable.svelte still exists (not deleted) in case other pages reference it
---

## 2026-03-02 - AP-009
- Added weight loss, AUC, and max RoR metric cards to session detail page
- Files changed:
  - `apps/dashboard/src/routes/sessions/[id]/+page.svelte` — added conditional metric grid between KeyTemperatures and PhaseStatsPanel
- **Learnings for future iterations:**
  - Frontend types (RoastSession in client.ts) already had all AP-001/AP-002 fields from earlier stories
  - SessionWithTelemetry extends RoastSession, so new fields are automatically available
  - Graceful hiding pattern: wrap entire section in `{#if field1 != null || field2 != null}` and individual cards in their own `{#if}`
---

## 2026-03-02 - AP-010
- Created roast comparison overlay page at /sessions/compare
- Files changed:
  - `apps/dashboard/src/routes/sessions/compare/+page.ts` (new) — page loader parsing ids from URL query params
  - `apps/dashboard/src/routes/sessions/compare/+page.svelte` (new) — comparison chart with time/event alignment, per-session colored BT+RoR curves, metadata summary table
  - `apps/dashboard/src/routes/sessions/+page.svelte` — added checkbox column for session selection, Compare button appears when ≥2 selected
  - `apps/dashboard/src/routes/sessions/[id]/+page.svelte` — added Compare... link button
- **Learnings for future iterations:**
  - Pass session IDs via URL query params: `/sessions/compare?ids=id1,id2,id3`
  - SvelteKit page load can access `url.searchParams` for query params
  - Event alignment: subtract the event's elapsed_seconds from all telemetry points
  - `events.list(id)` returns RoastEvent[] — must be loaded separately from sessions.get()
  - ECharts handles negative x-axis values well for event-aligned charts
  - Set mutation in Svelte 5: create new Set from old, modify, reassign (selectedIds = next)
---
