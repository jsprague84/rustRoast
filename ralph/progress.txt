## Codebase Patterns
- Settings table uses inline CREATE TABLE in init_db() (not migration files) since it's a simple key-value table
- Backend handlers in main.rs follow pattern: async fn handler(State(state): State<AppState>, ...) -> Response with match on Result
- Frontend API client uses typed namespace objects (e.g., `settings`, `profiles`) with method functions calling `request<T>(path, options, requiresAuth?)`
- SQLite upsert uses INSERT ... ON CONFLICT(key) DO UPDATE SET ... pattern
- Auth is frontend-only (Bearer header via requiresAuth flag); no backend middleware yet
- Svelte 5 rune stores use `.svelte.ts` extension with exported `$state` objects; pure functions exported separately for testability
- Profile/chart types (`ProfileWithPoints`, `ProfilePoint`) live in `$lib/api/client.ts` — import from there
- Chart.svelte exposes `getInstance()` for attaching ECharts event handlers (getZr click, series click)
- ECharts `convertFromPixel('grid', [offsetX, offsetY])` converts pixel click coords to data coordinates
- Use `getZr().on('click')` with `!event.target` check for blank area clicks (add points)
- Use `chart.on('click')` with `componentType === 'series'` for clicking on data points
- Svelte 5 `$props()` values referenced outside reactive contexts trigger `state_referenced_locally` warning — extract to plain `const` variables to make intent clear
- WebSocket messages are polymorphic: check for `msg.telemetry` vs `msg.autotune` key to determine type — parse as generic JSON first
- Autotune API endpoints: start/stop/apply require auth (POST), status/results latest are public (GET); API client namespace is `autotune` in client.ts

# Ralph Progress Log
Started: Sun Mar  1 05:07:32 PM CST 2026
---

## 2026-03-01 - PF-002
- What was implemented: Global settings table and API
  - Created `settings` table (key TEXT PK, value TEXT, updated_at) in init_db() with default `profile_lookahead_seconds=20`
  - GET /api/settings endpoint returns all settings as `{ key: value }` JSON object (public)
  - PUT /api/settings/:key endpoint upserts a single setting value (protected via frontend auth)
  - Frontend API client: `settings.get()` and `settings.set(key, value)` methods added to client.ts
- Files changed:
  - `crates/server/src/main.rs` — init_db table creation, route registration, handler functions
  - `apps/dashboard/src/lib/api/client.ts` — settings API namespace
- **Learnings for future iterations:**
  - Inline tables in init_db use CREATE TABLE IF NOT EXISTS + direct sqlx::query (no migration file needed for simple tables)
  - PUT handlers returning 204 use `StatusCode::NO_CONTENT.into_response()`
  - Frontend `request<void>` with 204 returns `undefined as T` (handled by the request helper)
  - Use `encodeURIComponent` on dynamic path segments in frontend API client
---

## 2026-03-01 - PF-003
- What was implemented: Profile store with Svelte 5 runes
  - Created `$lib/stores/profile.svelte.ts` using Svelte 5 `$state` runes
  - Reactive state object: `profileState` with `activeProfile`, `isFollowing`, `lastSentSetpoint`
  - Action functions: `loadProfile(id)`, `unloadProfile()`, `startFollowing()`, `stopFollowing()`
  - Pure `interpolateTarget(points, elapsedMs, lookaheadMs)` function for linear interpolation between sorted profile points
  - Handles boundary conditions: holds first/last point temps, returns null for empty points
- Files changed:
  - `apps/dashboard/src/lib/stores/profile.svelte.ts` — new file
- **Learnings for future iterations:**
  - Svelte 5 rune stores use `.svelte.ts` extension and export `$state` objects at module level
  - Cannot reassign exported `$state` — use an object with mutable properties instead (e.g., `profileState.activeProfile = ...`)
  - Pure functions (like `interpolateTarget`) should be exported separately from reactive state for testability
  - Profile points use `time_seconds` (number) — convert to ms by multiplying by 1000 for chart compatibility
  - Types `ProfileWithPoints` and `ProfilePoint` are defined in `$lib/api/client.ts`, not in a separate types file
---

## 2026-03-01 - PF-004
- What was implemented: Profile designer with visual chart editor
  - Created `ProfileDesigner.svelte` component with ECharts scatter+line chart
  - Click on blank chart area to add new profile points (time/temp)
  - Click existing points to select, edit via input fields, delete via button or keyboard Delete key
  - Table view toggle: switch between chart and editable data table for all points
  - Profile metadata inputs: name (required), description, charge_temp, target_end_temp
  - Save button calls `profiles.create()` for new or `profiles.update()` for existing
  - Cancel button navigates back to /profiles
  - New routes: `/profiles/new` and `/profiles/[id]/edit` with load function
  - Added "New Profile" and "Edit" buttons to profiles list page
- Files changed:
  - `apps/dashboard/src/lib/components/ProfileDesigner.svelte` — new component
  - `apps/dashboard/src/routes/profiles/new/+page.svelte` — new route
  - `apps/dashboard/src/routes/profiles/[id]/edit/+page.ts` — load function
  - `apps/dashboard/src/routes/profiles/[id]/edit/+page.svelte` — edit route
  - `apps/dashboard/src/routes/profiles/+page.svelte` — added New/Edit buttons
- **Learnings for future iterations:**
  - Chart.svelte's `getInstance()` returns the ECharts instance for attaching event handlers
  - Use `getZr().on('click')` + `convertFromPixel('grid', ...)` for blank area click-to-add
  - Use `chart.on('click')` with series name filter for selecting existing points
  - ECharts tooltip formatter must use `unknown` param type (not specific interface) due to ECOption type constraints
  - Svelte 5 props referenced at top level trigger `state_referenced_locally` warnings — intentional for initialization-only values; extract to `const` to document intent
  - `$state` arrays support `.push()`, `.splice()`, `.sort()` mutations directly (Svelte 5 proxies)
  - Use `bind:this={chartComponent}` then `chartComponent.getInstance()` to access Chart internals
---

## 2026-03-01 - PF-005
- What was implemented: Profile overlay on telemetry chart
  - Imported `profileState` from profile store into TelemetryChart.svelte
  - When `activeProfile` has points, adds a 'Profile' series: dashed purple (#a855f7) line on the temperature Y-axis
  - Profile points converted from seconds to milliseconds to match telemetry X-axis
  - Profile temperatures included in Y-axis bounds calculation so the axis auto-scales correctly
  - Profile series conditionally spread into the series array (no extra series when no profile loaded)
- Files changed:
  - `apps/dashboard/src/lib/components/TelemetryChart.svelte` — added import, profile data extraction, profile series
- **Learnings for future iterations:**
  - Use spread operator `...(condition ? [item] : [])` to conditionally include series in the ECharts series array
  - Profile overlay uses yAxisIndex: 0 (temperature axis, same as BT/ET) — no need to specify it since 0 is the default
  - `profileState.activeProfile` is reactive in `$derived.by` — reading it inside the derivation auto-tracks changes
  - Include profile temps in allTemps for Y-axis bounds so the chart scales to show the full profile curve
---

## 2026-03-01 - PF-006
- What was implemented: Profile following (auto setpoint control)
  - Created `FollowProfile.svelte` component with follow/stop toggle button
  - Button only enabled when a profile is loaded AND session is active (status === 'active')
  - Uses `$effect` with `setInterval(1000)` to run every second while following
  - Each tick interpolates target temp at (elapsed_ms + lookahead_ms) using `interpolateTarget()`
  - Sends POST setpoint via `control.setSetpoint()` only when |new_target - lastSentSetpoint| >= 1.0°C (hysteresis)
  - Automatically calls `control.setMode(deviceId, 'auto')` when following starts
  - Following auto-stops when session ends, profile is unloaded, or user toggles off
  - Lookahead loaded from settings API (`profile_lookahead_seconds`, default 20s)
  - Shows current status: lookahead value, last sent target temperature
  - Integrated into dashboard sidebar between SessionControls and LandmarkPanel
- Files changed:
  - `apps/dashboard/src/lib/components/FollowProfile.svelte` — new component
  - `apps/dashboard/src/routes/+page.svelte` — imported and placed FollowProfile in sidebar
- **Learnings for future iterations:**
  - `$effect` cleanup pattern works for intervals: return `() => clearInterval(id)` from the effect body
  - Profile store's `lastSentSetpoint` serves as hysteresis state — set it before the async API call to avoid duplicate sends
  - Compute elapsed time from `activeSession.start_time` via `Date.now() - new Date(start_time).getTime()` — don't rely on a separate timer
  - `$derived` works well for computed enable/disable conditions that depend on multiple reactive sources
---

## 2026-03-01 - PF-007
- What was implemented: Profile temperature delta in current readings
  - Added `activeSession` prop to `CurrentReadings.svelte` (passed from dashboard page)
  - Imported profile store and `interpolateTarget` to compute target temp at current elapsed time
  - Delta = actual BT - profile target (positive = ahead/hotter, negative = behind/cooler)
  - Display format: '+2.3°C' or '-1.5°C' with sign always shown via `fmtDelta()` helper
  - Color coding via `$derived`: text-green-400 (|delta| ≤ 2°C), text-amber-400 (2-5°C), text-red-400 (>5°C)
  - Delta card conditionally rendered: only shown when profile loaded AND session active AND delta computed
  - Updates in real-time on each telemetry tick (Date.now() recomputed whenever `$telemetry` changes)
- Files changed:
  - `apps/dashboard/src/lib/components/CurrentReadings.svelte` — added imports, props, delta logic, conditional card
  - `apps/dashboard/src/routes/+page.svelte` — pass `activeSession` prop to CurrentReadings
- **Learnings for future iterations:**
  - `$derived.by` with `Date.now()` inside works for elapsed-time calculations because `$telemetry` changes trigger recomputation (no need for a separate timer)
  - `interpolateTarget(points, elapsedMs, 0)` with lookahead=0 gives the actual profile target at the current time (vs lookahead for following)
  - Adding a prop to an existing component requires updating all call sites — check the dashboard page and any other consumers
  - Conditional grid items work cleanly with `{#if}` inside the grid — Tailwind grid auto-fills the extra cell
---

## 2026-03-01 - PF-009
- What was implemented: Artisan import redirects to profile designer for review/editing
  - Changed `handleImport()` in profiles page to redirect to `/profiles/{id}/edit` after successful import
  - Imported profile opens in ProfileDesigner so user can review and edit points before finalizing
  - Added `goto` import from `$app/navigation` for redirect
  - Success notification guides user to review imported data
  - Error notifications for invalid files already handled by existing code
- Files changed:
  - `apps/dashboard/src/routes/profiles/+page.svelte` — modified handleImport to redirect to edit page
- **Learnings for future iterations:**
  - The simplest Artisan import-to-edit flow: import via API (saves to DB), then redirect to `/profiles/{id}/edit` — avoids client-side parsing
  - `profiles.importArtisan()` returns `RoastProfile` with `id` — use it for the redirect path
  - The ProfileDesigner edit route's `+page.ts` load function fetches the full profile with points, so the redirect "just works"
---

## 2026-03-01 - PF-008
- What was implemented: Profile selector in session creation flow
  - Added `profile_id` field to frontend `RoastSession` and `CreateSessionRequest` interfaces (backend already had it)
  - Profile dropdown in SessionControls.svelte: fetches profile list on mount, shows in both quick-start and detailed form views
  - Dropdown shows profile name with charge temp and duration metadata
  - Selected `profile_id` included in POST /api/sessions request body
  - Auto-loads profile into profile store when session starts (both quick-start and form paths)
  - Auto-loads profile on page refresh if active session has a `profile_id`
  - User can start session without selecting a profile (optional, defaults to "No profile")
  - Select styling matches DeviceSelector pattern (bg-card, border-border, text-foreground, appearance-none)
- Files changed:
  - `apps/dashboard/src/lib/api/client.ts` — added `profile_id` to `RoastSession` and `CreateSessionRequest` interfaces
  - `apps/dashboard/src/lib/components/SessionControls.svelte` — profile list fetch, dropdown UI, auto-load on session start
- **Learnings for future iterations:**
  - Backend `CreateSessionRequest` and `RoastSession` already had `profile_id` — always check backend models before adding new fields
  - Profile list fetch uses `$effect(() => { fetchProfiles(); })` pattern — same as checkActiveSession
  - `loadProfile(...).catch(() => {})` is the fire-and-forget pattern for non-critical async loads
  - Profile dropdown uses `bind:value={selectedProfileId}` with empty string as "no selection" (maps to `undefined` via `|| undefined`)
---

## 2026-03-01 - AT-001
- What was implemented: Autotune store for state management
  - Created `$lib/stores/autotune.svelte.ts` using Svelte 5 `$state` runes
  - Reactive state: `autotuneState` with `status` (phase, stepCount, progress), `results` (Kp, Ki, Kd), `isAutotuning`, `targetTemp`
  - WebSocket event subscription via `autotuneEvent` readonly store from telemetry.ts
  - Parses status events (phase, step_count → progress estimate at ~15 cycles) and results events (Kp, Ki, Kd)
  - Actions: `startAutotune()`, `stopAutotune()`, `applyResults()`, `dismissResults()`
  - `fetchLatestAutotune(deviceId)` for page-refresh recovery via GET status/results/latest endpoints
  - `initAutotuneSubscription()` / `destroyAutotuneSubscription()` for lifecycle management
  - Added `autotune` API namespace to client.ts: start, stop, apply, getLatestStatus, getLatestResults
  - Added `AutotuneWsMessage` type to telemetry.ts
  - Modified telemetry store to forward autotune WS events via `_autotuneEvent` writable + `autotuneEvent` readonly export
- Files changed:
  - `apps/dashboard/src/lib/stores/autotune.svelte.ts` — new file
  - `apps/dashboard/src/lib/api/client.ts` — autotune API namespace + interfaces
  - `apps/dashboard/src/lib/stores/telemetry.ts` — autotune event forwarding
  - `apps/dashboard/src/lib/types/telemetry.ts` — AutotuneWsMessage interface
- **Learnings for future iterations:**
  - The WebSocket sends both telemetry (`{ device_id, telemetry }`) and autotune (`{ device_id, autotune: { type, data } }`) messages — parse `msg` as generic JSON first, then branch on presence of `telemetry` vs `autotune` key
  - Autotune events flow: ESP32 → MQTT `roaster/{id}/autotune/{status|results}` → server caches + forwards to WS → frontend store
  - Svelte writable stores can bridge between legacy Svelte stores (telemetry.ts) and Svelte 5 rune stores (.svelte.ts) — subscribe in the rune store to the writable's `subscribe()` method
  - Autotune endpoints use `true` for requiresAuth (start/stop/apply are protected) while status/results queries are public
  - Progress estimation: divide step_count by 15 (typical relay cycle count), cap at 95% until ANALYZING phase
---

## 2026-03-01 - AT-002
- What was implemented: Autotune control panel on settings page
  - Created `AutotunePanel.svelte` component with target temperature input (150-250°C, default 200, step 5)
  - Start Autotune button (amber, Play icon) calls `startAutotune(deviceId, targetTemp)` from the autotune store
  - Stop Autotune button (red, Square icon) shown while autotuning, calls `stopAutotune(deviceId)`
  - Panel disabled with message when no device connected
  - Warning banner shown when a roast session is active (checks sessions API)
  - Start button disabled via `$derived` combining: isAutotuning, hasActiveSession, deviceId presence, loading state
  - Initializes autotune WS subscription and fetches latest status on mount (page refresh recovery)
  - Integrated into settings page as a new section below API Key settings
- Files changed:
  - `apps/dashboard/src/lib/components/AutotunePanel.svelte` — new component
  - `apps/dashboard/src/routes/settings/+page.svelte` — import AutotunePanel, add to layout
- **Learnings for future iterations:**
  - The settings page had `mt-6` margin on the first card section — switched to `space-y-6` on the container for consistent spacing between sections
  - `deviceId` from telemetry store works on the settings page (WS connects app-wide) — no need to add a DeviceSelector
  - Active session check via `sessions.list()` is the simplest approach for pages that don't have session state in scope
  - `$effect` with cleanup pattern works for both `initAutotuneSubscription` and `destroyAutotuneSubscription` lifecycle
  - Label `for` attribute + input `id` ensures accessibility (no a11y warnings for this component)
---

## 2026-03-01 - AT-003
- What was implemented: Real-time autotune progress display in AutotunePanel
  - Phase display with color coding: amber (heating/stabilizing), green (running/complete), blue (analyzing), red (error)
  - Phase label shown as title-case (e.g., "Heating", "Running")
  - Step count shown during RUNNING phase ("Step 7 of ~15")
  - Animated progress bar with phase-appropriate color and smooth transitions
  - Current BT reading from telemetry store and target temperature from autotune store
  - Error state shows descriptive error message from autotune event data
  - All state from autotune store (WebSocket-driven, no polling)
- Files changed:
  - `apps/dashboard/src/lib/components/AutotunePanel.svelte` — added telemetry import, $derived phase/color computations, progress display section
- **Learnings for future iterations:**
  - `$derived.by` works well for switch-based mappings (phase → color class) since the derivation auto-tracks the reactive source
  - Tailwind dynamic width via inline `style="width: {value}%"` works for progress bars (class-based widths can't be dynamic)
  - `transition-all duration-500` on the progress bar div gives a smooth animation as progress updates arrive via WebSocket
  - Import `telemetry` (readonly store) from telemetry.ts to access current BT in any component with `$telemetry?.beanTemp`
---

## 2026-03-01 - AT-004
- What was implemented: Autotune results display with apply/discard functionality
  - Results section in AutotunePanel shows after autotune completes (when `autotuneState.results` is set and `isAutotuning` is false)
  - Comparison table with Current (from telemetry Kp/Ki/Kd) vs Recommended (from autotune results) columns
  - Apply button calls `applyResults(deviceId)` → POST /api/roaster/:device_id/autotune/apply, then shows success notification and dismisses results
  - Discard button calls `dismissResults()` to clear the results display
  - Failure notification shown if apply API call fails
  - Results persist in store until explicitly dismissed (survive re-renders)
  - On page load, `fetchLatestAutotune(deviceId)` already fetches both status and results from the backend
  - Styled with green accent (bg-green-500/10, border-green-500/30) to indicate success
- Files changed:
  - `apps/dashboard/src/lib/components/AutotunePanel.svelte` — added imports (Check, X, applyResults, dismissResults), handleApply/handleDiscard functions, results comparison table, apply/discard buttons
- **Learnings for future iterations:**
  - Telemetry interface has optional `Kp`, `Ki`, `Kd` fields — use `$telemetry?.Kp != null` for null-safe access
  - Ki values are typically very small (0.0001-0.01 range) — use `.toFixed(4)` for Ki vs `.toFixed(2)` for Kp/Kd
  - The `fetchLatestAutotune` function already handles loading both status and results on page load — no additional fetch needed for results recovery
  - Place the results section outside the `isAutotuning` conditional but still inside the main controls `div` — results should show alongside the Start button (not during active autotuning)
---

## 2026-03-01 - AT-005
- What was implemented: Autotune target line annotation on telemetry chart
  - Imported `autotuneState` from autotune store into TelemetryChart.svelte
  - When `isAutotuning` is true OR `results` are pending (not dismissed), AND `targetTemp` is set, adds a horizontal dashed markLine at the target temperature
  - Line styled as orange (#f97316) dashed line labeled 'AT Target'
  - Uses the same `markLines` array as landmark annotations — merged into BT series markLine data
  - Autotune target temperature included in Y-axis bounds calculation so chart auto-scales to show it
  - Existing BT, ET, RoR, Heater, Fan, and Profile series continue to render normally
  - Defined explicit `MarkLineItem` type to support both xAxis (landmarks) and yAxis (autotune) markLines in the same array
- Files changed:
  - `apps/dashboard/src/lib/components/TelemetryChart.svelte` — added autotuneState import, MarkLineItem type, autotune target markLine logic, Y-axis bounds inclusion
- **Learnings for future iterations:**
  - ECharts markLine with `yAxis: value` creates a horizontal line (vs `xAxis: value` for vertical landmark lines)
  - When mixing xAxis and yAxis markLines in the same array, define an explicit union type (`MarkLineItem`) with both optional to avoid TypeScript errors
  - Include markLine target values in allTemps for Y-axis bounds calculation to ensure the line is always visible on the chart
  - `autotuneState` is reactive in `$derived.by` blocks — reading `isAutotuning`, `results`, and `targetTemp` inside the derivation auto-tracks changes
---
